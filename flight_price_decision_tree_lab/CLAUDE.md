# 공통 작업 규칙

- 모든 경로는 프로젝트 루트를 기준으로 한 상대경로를 사용한다.
- 원본 데이터는 `data/raw/`에서 직접 수정하거나 덮어쓰지 않는다.
- 가공한 데이터가 필요하면 `data/processed/`에 원본과 구분되는 이름으로 저장한다.
- 분석용 Python 파일을 만들 때는 `analysis/`에 저장한다.
- 표는 `outputs/tables/`, 그래프는 `outputs/figures/`에 저장한다.
- 같은 입력에서 같은 결과를 재현할 수 있도록 난수와 데이터 분할 기준을 명시한다.
- 분석 전에 실제 파일명, 열 이름, 자료형과 행 수를 확인한다.
- 제외하거나 대체한 데이터와 변수의 기준 및 건수를 기록한다.
- 식별자 또는 예측 시점 이후에만 알 수 있는 정보가 입력변수에 포함되지 않도록 확인한다.
- 원본 데이터와 가공 데이터의 관측 단위가 달라지면 그 이유와 변환 기준을 기록한다.
- 요청받지 않은 Markdown 문서, 임시 파일과 중간 산출물을 만들지 않는다.
